<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.yedam.my.MyMapper">

	<select id="qnaList" resultType="qna">
		select inq_no,
		       inq_type,
		       inq_title,
		       inq_content,
		       inq_date,
		       inq_resp,
		       mem_no
		from inquiries
	</select>
	
	<select id="selectQna" resultType="qna" parameterType="int">
		select inq_no,
		       inq_title,
		       inq_content,
		       inq_date,
		       mem_no
		from inquiries
		where inq_no = #{inqNo}
	</select>
	
	<select id="selectMember" parameterType="member">
		select mem_no,
		       mem_id,
		       mem_pw,
		       mem_name,
		       mem_phone,
		       mem_mail,
		       mem_addr,
		       mem_point
		from members
		where mem_id = #{memId}
		and   mem_pw = #{memPw}
	</select>

</mapper>
